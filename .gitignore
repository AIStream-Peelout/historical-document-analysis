*.pyc
*.log
*.DS_STORE
wandb/
*.env
src/datasets/raw_data
.env
src/models/llm/transcription_raw_outputs
src/models/llm/genizah_images
.idea
/.idea
idea/
*.txt
genizah_images
transcription_raw_outputs
transcription_results
# Qwen Hebrew fine-tuning artifacts (large, regenerable — canonical copies on HF hub)
src/datasets/processed/
models/
.venv-mlx/
src/finetuning/qwen_hebrew/assets/

# W&B artifact downloads (regenerable from the W&B API; thousands of PNG/table files)
artifacts/
**/artifacts/

# Evaluation outputs: raw model transcriptions, downloaded page/fragment images,
# and per-run result dumps. All regenerable or archived in W&B.
**/genizah_images/
**/transcription_raw_outputs/
transcription_raw_outputs_archive_*/
**/transcription_raw_outputs_archive_*/
**/transcription_results/
**/talmud_results/

# Paper source lives in the separate genizah-paper repository (split 2026-08-05).

# built grounding viewer (13 MB, embeds page images) — rebuild with viewer/build_viewer.py
src/datasets/evaluations/grounding_eval/viewer/grounding_viewer.html

# the Space needs its pip manifest despite the blanket *.txt rule above
!spaces/genizah_reader/requirements.txt

# article export scratch (headless Firefox profile, superseded exports)
article/build/ffprofile/
article/build/assets_prev_*/

# column-transcription probe raw cache (crops + per-column decodes)
src/datasets/evaluations/transcription_raw_outputs_probe_columns/
.env.bak*
.claude/worktrees/
.claude/settings.local.json
logs