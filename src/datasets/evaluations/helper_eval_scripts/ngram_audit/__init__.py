"""Audit tooling for the order-independent n-gram precision metric.

Everything here is a *variant* of ``score_genizah_offline.py`` behind explicit
parameters.  The default configuration reproduces the paper numbers exactly
(checked by ``score_variant.py --check-paper``); the default scoring path in
``score_genizah_offline.py`` is never modified.
"""
